name: No Concurrency Demo

on:
  push:

concurrency:
  group: branch-${{ github.ref }}
  cancel-in-progress: true

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          chmod +x script.sh
          ./script.sh


